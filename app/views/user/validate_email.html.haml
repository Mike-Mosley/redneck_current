#registration
  %table#registration_table
    %tbody
      %tr
        %td
          %h3 VALIDATION
          - if @validated == true
            = image_tag ('validation.jpg', :size => '576x432')
          - else
            Something went wrong. Please contact administrator